static void sortCols(int[][] arr){
    int rows = arr.length, cols = arr[0].length;
    for(int j=0; j<cols; j++){
        int[] col = new int[rows];
        for(int i=0; i<rows; i++) col[i] = arr[i][j];
        Arrays.sort(col);
        for(int i=0; i<rows; i++) arr[i][j] = col[i];
    }
}
